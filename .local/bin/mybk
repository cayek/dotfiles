#!/usr/bin/env bash

HOST=$(hostname)
bk ()
{
    rsync -avzL --delete \
        "${1}" "bk:/mnt/red/Backup/${HOST}/"
    echo `date` ": ${1} ok" | ssh bk -T "cat >> /mnt/red/Backup/${HOST}.log"
}

bk ~/.Private
bk ~/cipher
bk ~/.gnupg
bk ~/.ssh
bk ~/.zsh_history
